# 内容校对清单

每次更新基础文档时检查：

- 命令是否明确适用平台和风险；
- 删除数据、修改权限和开放网络的操作是否有警告；
- 版本敏感内容是否引用官方文档；
- `docGroup` 是否存在于 `src/config/docs.ts`；
- `docSection` 和 `docOrder` 是否保持目录顺序；
- `published` 与 `updated` 是否符合日期说明；
- 代码块是否可以独立理解；
- 文档是否避免把密钥和弱密码写入仓库。
